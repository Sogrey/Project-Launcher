# 本地项目启动器 (Project Launcher) 产品需求文档 (PRD)

| 项目 | 内容 |
| :--- | :--- |
| **文档版本** | V1.1 |
| **项目名称** | Project Launcher |
| **目标用户** | 经常在本地同时启动多个 Node.js / 前端项目的开发者 |
| **技术栈** | Vue 3 + TypeScript + Pinia + Vite · Tauri 2 + Rust · xterm.js · tauri-plugin-store |
| **UI** | 深色科技风（网格底纹），三栏看板 |
| **当前交付** | 应用版本 **1.0.0**（功能以本文「已实现」为准） |

---

## 一、核心目标

* **痛点：** 告别在多个 IDE 终端窗口间来回找日志、手动启停一堆 `dev` 服务。
* **一键管理：** 可视化看板完成项目分组、多脚本并行启停、实时日志、异常回看、端口跳转。
* **独立运行：** 不依赖某个 IDE 窗口；可托盘常驻，作为本地「项目进程管家」。

---

## 二、产品模型（与早期 PRD 的差异）

| 维度 | 早期设想 (V1.0 草案) | **当前实现** |
|------|----------------------|--------------|
| 工作区 | 选一个本地根目录自动扫描 | **命名工作区**（分组名，非目录）；多工作区切换 |
| 看板 | 待处理 / 进行中 / 异常（项目级流转） | **项目列表常驻** \| **运行中（项目+脚本）** \| **日志 + 异常** |
| 运行粒度 | 一项目一进程 | **同一项目可并行多个脚本**（`runId = pathId@script`） |
| 日志入口 | 卡片展开 / 独立窗 | **右侧日志格**；抽屉只管项目，不内嵌日志 |
| 发现方式 | 仅扫描工作区根目录 | **单项目添加** + **从目录批量导入** |

---

## 三、功能需求（按开发现状）

图例：✅ 已实现 · ⬜ 未实现 / 规划中

### 1. 工作区与项目库

| ID | 需求 | 状态 | 说明 |
|----|------|------|------|
| 1.1 | 命名工作区（创建 / 切换 / 重命名 / 删除） | ✅ | 至少保留 1 个；默认「默认」 |
| 1.2 | 工作区配置本地持久化 | ✅ | `tauri-plugin-store` JSON；重启恢复 |
| 1.3 | 切换工作区前停止当前运行中的进程 | ✅ | |
| 1.4 | 新增单个项目（含 `package.json` 的目录） | ✅ | |
| 1.5 | 从根目录递归扫描并导入当前工作区 | ✅ | 深度约 3；识别 `package.json` |
| 1.6 | 从工作区移除项目（不解绑磁盘文件） | ✅ | 确认框；运行中先停 |
| 1.7 | 解析 `package.json` scripts | ✅ | |
| 1.8 | 过滤伪注释脚本（`//`、`#`、`@` 等） | ✅ | 前后端规则一致 |
| 1.8a | 启动时后台同步 `package.json` 脚本列表 | ✅ | 不在开抽屉时同步，避免点击卡顿 |
| 1.9 | 推荐脚本排序（dev / serve / start / build） | ✅ | 抽屉脚本列表 |
| 1.10 | 项目列表拖拽排序并持久化 | ✅ | HTML5 DnD；Tauri `dragDropEnabled: false` |
| 1.11 | 单根目录「工作区路径」自动绑定 | ⬜ | 已由命名工作区模型取代 |
| 1.12 | 项目显示别名 | ⬜ | 当前用文件夹/扫描名 |
| 1.13 | 手动配置默认端口 | ⬜ | 现为日志自动提取 |
| 1.14 | 按项目注入环境变量 | ⬜ | 规划中 |
| 1.15 | 非 Node 项目（Python / Go / Java） | ⬜ | V2+ |

### 2. 看板控制台

| ID | 需求 | 状态 | 说明 |
|----|------|------|------|
| 2.1 | 顶栏统计：总项目 / 运行中 / 异常 / 今日启动 | ✅ | 「今日启动」偏会话计数，跨日持久化未做 |
| 2.2 | 左栏：项目列表（启动后仍保留） | ✅ | |
| 2.3 | 中栏：运行中（每条 = 项目 + 脚本） | ✅ | 可点选日志焦点；■ 停止；端口链接 |
| 2.4 | 右上：日志输出 | ✅ | xterm；取消焦点 / 自动滚动 / 清空 |
| 2.5 | 右下：异常记录 | ✅ | 点击在右上看日志（不打开抽屉）；可清除 |
| 2.6 | 一键全停 | ✅ | 顶栏 + 托盘 |
| 2.7 | 项目级「待处理→进行中→异常」三列流转 | ⬜ | 已废弃该模型 |

### 3. 项目管理抽屉

| ID | 需求 | 状态 | 说明 |
|----|------|------|------|
| 3.1 | 点击左侧项目打开右侧抽屉 | ✅ | |
| 3.2 | 运行状态表（运行中 / 异常脚本） | ✅ | 停止 / 清除 |
| 3.3 | 包管理器选择 + install | ✅ | 按项目：`package.json#packageManager` → 锁文件 → 默认 npm；可手动改并持久化 |
| 3.4 | 脚本启停 / 重启（可并行） | ✅ | |
| 3.5 | 停止该项目全部脚本 | ✅ | |
| 3.6 | 在资源管理器中打开项目目录 | ✅ | |
| 3.7 | 用已安装 IDE 打开项目 | ✅ | 见 §5 |
| 3.8 | 抽屉内嵌日志 | ⬜ | 刻意不做；日志在主界面右侧 |

### 4. 进程与运行模型

| ID | 需求 | 状态 | 说明 |
|----|------|------|------|
| 4.1 | 多脚本并行（同项目） | ✅ | |
| 4.2 | `runId = pathId@script` 隔离日志/端口/停止 | ✅ | |
| 4.3 | 同时运行上限 | ✅ | 默认 20 |
| 4.4 | 停止时杀掉进程树 | ✅ | 避免子进程占端口 |
| 4.5 | 重启防陈旧 stop/exit 事件 | ✅ | epoch 机制 |
| 4.6 | `FORCE_COLOR=1` 彩色日志 | ✅ | |
| 4.7 | 日志批量刷到前端（时间窗 / 行数） | ✅ | ~200ms / 50 行 |
| 4.8 | 从该脚本日志提取端口并跳转浏览器 | ✅ | 按 run 隔离 |
| 4.9 | 应用重启后自动恢复上次运行中的进程 | ⬜ | V2 |

### 5. 日志体验

| ID | 需求 | 状态 | 说明 |
|----|------|------|------|
| 5.1 | xterm.js 渲染（非纯文本拼接） | ✅ | + FitAddon；scrollback 5000 |
| 5.2 | 自动滚动 / 清空日志 | ✅ | |
| 5.3 | 智能日志焦点（`focusRunIfIdle`） | ✅ | 有焦点时批量启动不乱跳 |
| 5.4 | 异常任务保留日志并可回看 | ✅ | |
| 5.5 | 长行可读（最小列宽 + 横向滚动） | ✅ | |
| 5.6 | 日志内关键字搜索 | ⬜ | V2 |
| 5.7 | 独立日志弹出窗 | ⬜ | 当前固定右上格 |

### 6. IDE 集成

| ID | 需求 | 状态 | 说明 |
|----|------|------|------|
| 6.1 | 检测本机已安装编辑器 | ✅ | VS Code / Cursor / Trae / Trae CN / TRAE SOLO CN / CodeBuddy CN·Intl / Windsurf / VSCodium |
| 6.2 | Windows：注册表 → PATH → 常见路径 | ✅ | 支持自定义安装路径；拒绝 Temp 等不安全路径 |
| 6.3 | 会话内缓存检测结果（避免每次开抽屉卡顿） | ✅ | 启动预热 + 10s 超时 |
| 6.4 | 用所选 IDE 打开项目目录 | ✅ | 启动前二次安全校验 |
| 6.5 | 记住上次选用的 IDE | ✅ | localStorage |

### 7. 系统集成

| ID | 需求 | 状态 | 说明 |
|----|------|------|------|
| 7.1 | 关闭主窗口 → 托盘常驻（不退出） | ✅ | |
| 7.2 | 托盘：显示主窗口 / 一键全停 / 退出 | ✅ | 退出前全停 |
| 7.3 | 开机自启开关 | ⬜ | V2 |

---

## 四、技术架构（已落地）

| 层级 | 选型 |
|------|------|
| 桌面壳 | **Tauri 2.x** |
| 前端 | **Vue 3 + Vite 5 + TypeScript + Pinia** |
| 日志 | **@xterm/xterm** + **@xterm/addon-fit** |
| UI 提示 | Element Plus Message（按需） |
| 持久化 | **tauri-plugin-store**（JSON，非 SQLite） |
| 进程 | Rust `std::process::Command` + 进程树清理 |
| 前置 | Node ≥ 18 · Rust ≥ 1.75 · Windows 需 MSVC 工具链 |

### 关键 IPC / 事件（摘要）

* 命令：`scan_project` / `scan_directory` / `start_project` / `install_project` / `stop_project` / `stop_all_projects` / 配置读写 / `list_installed_ides` / `open_in_explorer` / `open_in_ide`
* 事件：`project:log` · `project:port` · `project:exited` · `project:stopped`（payload 中的 id 实为 **runId**）

---

## 五、非功能性需求

| 项 | 目标 | 现状备注 |
|----|------|----------|
| 性能 | 主界面轻量；多项目并行时 UI 可用 | 日志缓冲 + xterm；scrollback 5000 需关注长跑内存 |
| 稳定性 | 停止后端口释放；无僵尸子进程 | 进程树 kill；停失败时 UI 与后端对齐 |
| 安全 | 路径校验；IDE 启动防注入 | 目录 canonicalize；IDE 文件名/关键词/非临时路径校验 |
| 引导 | 首次使用清晰引导 | 当前靠顶栏「工作区 / 新增项目」；无独立引导页 |

---

## 六、版本规划（修订）

### 已交付 · V1.0（当前）

* 命名多工作区 + 项目导入/拖拽排序  
* 三栏看板（项目列表 \| 运行中 \| 日志/异常）  
* 多脚本并行、install、一键全停、托盘常驻  
* xterm 彩色日志、端口跳转、异常回看  
* 打开目录 + 检测并用 IDE 打开  

### 规划 · V1.x / V2.0

* 项目别名、按项目包管理器与环境变量  
* 日志搜索 / 错误高亮  
* 开机自启；可选「重启后恢复运行中任务」  
* 「今日启动」跨日持久化  
* bun；非 Node 技术栈  
* （可选）首次使用引导页  

---

## 七、验收对照（V1.0 最小集）

- [x] 可创建并切换命名工作区，配置重启后仍在  
- [x] 可添加/导入 Node 项目并过滤非法 scripts  
- [x] 可并行启动同项目多脚本，运行中列表按脚本展示  
- [x] 可停止单脚本 / 项目全部 / 全局全停，子进程不残留占端口  
- [x] 右侧可看彩色实时日志；异常可回看  
- [x] 检测到端口可一键打开浏览器  
- [x] 抽屉可打开目录、用本机 IDE 打开项目  
- [x] 关闭窗口进托盘，托盘可全停与退出  

---

**说明：** 本文 V1.1 以仓库当前实现与 `README.md` 为准，取代草案中「单根目录工作区 + 项目级三列流转」等已变更设计。后续需求变更请同步更新本节状态表与版本规划。
